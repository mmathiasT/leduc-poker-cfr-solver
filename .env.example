# Copy to .env and fill in for anything beyond local dev (production, staging).
# These are read as plain environment variables (webapp/settings.py) — export
# them yourself or use a tool like `direnv`/`python-dotenv` to load this file.

DJANGO_SECRET_KEY=
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=example.com,www.example.com
